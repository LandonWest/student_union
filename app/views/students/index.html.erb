<h1>Home Page</h1>

<%= link_to('Sign Up for Course', new_student_path) %>

<ul>
  <% @students.each do |student| %>
  <li>
    <%= link_to(student.name, student_path(student.id)) %>
    <%= student.course %>
  </li>
  <% end %>
</ul>
